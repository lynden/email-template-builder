{
  "name": "email-builder",
  "version": "1.0.0",
  "description": "A build process for creating emails.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lynden Oliver",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.10.0",
    "gulp": "^3.9.0",
    "gulp-inline-css": "^3.0.0",
    "gulp-sass": "^2.1.0"
  }
}
